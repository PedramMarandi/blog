import React from "react"
import { graphql, links } from "gatsby"

const SingleTagsTemplates = ({ data, pageContext }) => {
  console.log("SingleTagsTemplates ", pageContext)
  return (
    <div>
      <div>single tags here</div>
    </div>
  )
}
export default SingleTagsTemplates
